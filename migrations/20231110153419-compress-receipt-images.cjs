module.exports = {
  /** @param {{ context: import('pg').Pool }} context */
  async up({ context: db }) {
    // not supported
  },

  /** @param {{ context: import('pg').Pool }} context */
  async down({ context: db }) {
    // not supported
  },
}
