module.exports = {
  /** @param {{ context: import('pg').Pool }} context */
  async up({ context: db }) {
    // stub
  },

  /** @param {{ context: import('pg').Pool }} context */
  async down({ context: db }) {
    // stub
  },
}
