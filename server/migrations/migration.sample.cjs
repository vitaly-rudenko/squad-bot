module.exports = {
  /** @param {{ context: import('pg').Pool }} context */
  async up({ context: db }) {
    // TODO: make changes
  },

  /** @param {{ context: import('pg').Pool }} context */
  async down({ context: db }) {
    // TODO: revert changes
  },
}
